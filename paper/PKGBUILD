# Maintainer: Josh Thomas <josh@joshthomas.dev>

pkgname=paper-design
pkgver=0.1.8
pkgrel=1
pkgdesc="The connected canvas for teams shipping with agents"
arch=(x86_64)
url="https://paper.design"
license=(custom:Proprietary)
depends=(
  gtk3
  libnotify
  nss
  libxss
  libxtst
  xdg-utils
  at-spi2-core
  util-linux-libs
  libsecret
)
optdepends=(
  'libappindicator-gtk3: system tray support'
)
provides=(paper-desktop)
options=(!strip !debug)
source=("paper-design-${pkgver}.deb::https://download.paper.design/linux/deb")
sha256sums=('014ae7fac7d5a6a2260bf50fb8babcff1132d33dad33ff88ebbc50f67351772c')

package() {
  cd "$srcdir"

  bsdtar -xf data.tar.* -C "$pkgdir/"

  # fix permissions on chrome-sandbox
  chmod 4755 "$pkgdir/opt/Paper/chrome-sandbox"

  # symlink binary to /usr/bin
  install -d "$pkgdir/usr/bin"
  ln -s /opt/Paper/paper-desktop "$pkgdir/usr/bin/paper-desktop"
}
